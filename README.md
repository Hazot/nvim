# Neovim Config

## Requirements

### Ubuntu
- `sudo apt install ripgrep fd-find`
- `cargo install fd-find`

### MacOS
- `brew install ripgrep fd sioyek`

### Arch Linux
- `yay -Syu ripgrep`
- `cargo install fd-find`
